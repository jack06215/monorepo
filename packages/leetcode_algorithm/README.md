# LeetCode Algorithm

Solutions are organized by problem (one directory per topic). Each problem
directory may contain a C++ implementation (`*.cpp`, tested via the vendored
Catch2 header) and/or a Python implementation (`*.py`, tested via stdlib
`unittest`).

```sh
bazel query "//packages/leetcode_algorithm:*"
```
