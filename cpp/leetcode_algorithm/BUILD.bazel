load("@rules_cc//cc:defs.bzl", "cc_binary", "cc_library")

cc_library(
    name = "prettyprint",
    srcs = ["prettyprint.h"],
)

[
    cc_binary(
        name = src[:-len(".cpp")].split("/")[-1],
        srcs = [src],
        deps = [":prettyprint"],
    )
    for src in glob(["**/*.cpp"])
]
